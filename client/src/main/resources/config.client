# Services that the client will be using.
Printer.Proxy=SimplePrinter:tcp -p 9099
ExecuteCommand.Proxy=SimpleCommandExecute:tcp -p 9098

# Uncomment to use the WebSocket transports instead.
#Hello.Proxy=hello:ws -p 10002:udp -p 10000:wss -p 10003

# We specify the server's hostname where the client
# will be connected.
Ice.Default.Host=hgrid2